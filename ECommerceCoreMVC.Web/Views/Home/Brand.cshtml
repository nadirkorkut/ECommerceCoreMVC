@model Brand
@{ 
    ViewData["Title"] = Model.Name;
}

<div class="p-2 mb-2">
    <div class="h5">
        @Model.Name
    </div>
</div>

<partial name="ProductListPartial" model="Model.Products.Where(p=>p.Enabled).ToList()" />